const constants = {
    SALT_ROUNDS : 10
}

const allowlist = ['http://localhost:3000', 'http://example1.com', 'http://example2.com']; // add more domains as needed



module.exports = {constants, allowlist}